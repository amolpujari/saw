Description:
    Explain the generator

Example:
    rails generate saw Thing

    This will create
     db/migrate/XXXXXXXXXXXXXXXX_add_visits_and_links.rb 
     route  post "visits" => "visits#create", :defaults => { :format => "json"}

